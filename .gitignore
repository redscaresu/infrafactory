# Generated output
output/

# Runtime data
.infrafactory/

# OpenTofu
.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
tfplan

# Go
/infrafactory
/bin/

# OS
.DS_Store

# Local review notes
ISSUES.md
issues.md

# Secrets
.env
.env.*
*.pem
*.key
credentials.json
service-account.json
token.json

# Claude Code
.claude/

# Web UI
ui/node_modules/
ui/build/
ui/.svelte-kit/
ui/test-results/
cmd/infrafactory/ui/build/

# Per-run Playwright artifacts from the UI demo recording.
docs/demo/walkthrough/

# UI demo source webms. The .gif is what the README embeds; the .webm
# is a regeneration-source kept around but not committed because longer
# recordings (e.g. the gcp-pubsub multi-iteration walkthrough at ~2min)
# blow past the 5MB pre-commit large-file guard. Re-record from source
# via `make demo-ui` / `make demo-ui-run`.
docs/demo/*.webm
